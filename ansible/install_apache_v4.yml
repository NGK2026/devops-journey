---

- hosts: all
  become: true
  tasks:

  - name: update index and install apache and php
    apt:
      name:
        - "{{ apache_package }}"
        - "{{ php_package }}"
      state: latest
      update_cache: true



