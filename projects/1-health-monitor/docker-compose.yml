# EC2 Amazon Linux v2023 docker does not support docker compose
# Compose approach neglected for this project.

# Create service,
services:
  # named health-monitor
  health-monitor:
    # built from Dockerfile in current directory
    build: .
    # with port 5000 open
    ports:
    - 5000:5000
    restart: always